# Xcode / macOS
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.moved-aside
*.pbxuser
!default.pbxuser

# Generated by XcodeGen — regenerate with `xcodegen generate`
Rota.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (not used, but just in case)
Pods/
Carthage/Build/

# Fastlane
fastlane/report.xml
fastlane/test_output/

# Python (logo generator) — the generated PNGs under design/out are kept so the
# icon can be regenerated or inspected without rerunning the script.
design/__pycache__/
